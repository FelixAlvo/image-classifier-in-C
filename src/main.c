#include <stdint.h>
#include "config.h"
#include "neural_net.h"
#include "utils_log.h"
#include "utils_math.h"

int main(){
    for (int i = 0; i< 20; i++){
        randf(RANDOM_SEED);
    }
    return 0;
}